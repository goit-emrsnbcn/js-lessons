//if you want to transform each item on the array
let numbers = [1, 2, 3, 4, 5];

let doubleOutputMap = numbers.map((num) => num * 2);
let doubleOutputFilter = numbers.filter((num) => num * 2);
console.log(doubleOutputMap);
console.log(doubleOutputFilter);
